name: Testing and Code Quality

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install Qt dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            libgl1 \
            libegl1 \
            libxkbcommon-x11-0 \
            libxcb-icccm4 \
            libxcb-image0 \
            libxcb-keysyms1 \
            libxcb-randr0 \
            libxcb-render-util0 \
            libxcb-xinerama0 \
            libxcb-xfixes0 \
            libxcb-shape0 \
            libxcb-cursor0 \
            libdbus-1-3 \
            libfontconfig1 \
            libglib2.0-0 \
            libgssapi-krb5-2 \
            libx11-6 \
            libx11-xcb1 \
            libxext6 \
            libxrender1 \
            libxkbcommon0
      
      - name: Install Python dependencies
        run: |
          pip install -r requirements.txt -r requirements-dev.txt
          pip install -e .
      
      - name: Check QML formatting
        run: |
          find . -name '*.qml' -not -path './.venv/*' -print0 | xargs -0 pyside6-qmlformat -i
          git diff --exit-code

      - name: Python unit tests with coverage
        env:
          QT_QPA_PLATFORM: offscreen
        run: |
          pytest --cov=. --cov-report=term --cov-report=xml --cov-report=html
      
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
        if: always()

